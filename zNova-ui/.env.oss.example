# OSS配置示例文件
# 复制此文件为 .env.local 或直接在 .env.development/.env.production 中配置

# 头像存储方式: local/oss
VITE_AVATAR_STORAGE_TYPE = 'oss'

# 阿里云OSS配置
# Endpoint: 例如 https://oss-cn-hangzhou.aliyuncs.com
VITE_OSS_ENDPOINT = 'your-oss-endpoint'

# Bucket名称
VITE_OSS_BUCKET = 'your-bucket-name'

# 存储区域: 例如 oss-cn-hangzhou
VITE_OSS_REGION = 'your-region'

# 自定义域名（可选，如果有绑定自定义域名）
# VITE_OSS_CUSTOM_DOMAIN = 'https://img.yourdomain.com'