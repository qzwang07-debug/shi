<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zNova.system.mapper.AppUserMapper">

    <resultMap type="com.zNova.common.core.domain.entity.AppUser" id="AppUserResult">
        <result property="userId"    column="user_id"    />
        <result property="username"    column="username"    />
        <result property="password"    column="password"    />
        <result property="nickname"    column="nickname"    />
        <result property="avatar"    column="avatar"    />
        <result property="sex"    column="sex"    />
        <result property="phonenumber"    column="phonenumber"    />
        <result property="status"    column="status"    />
        <result property="delFlag"    column="del_flag"    />
        <result property="loginIp"    column="login_ip"    />
        <result property="loginDate"    column="login_date"    />
        <result property="creditScore"    column="credit_score"    />
        <result property="balance"    column="balance"    />
        <result property="frozenDeposit"    column="frozen_deposit"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="remark"    column="remark"    />
    </resultMap>

    <sql id="selectAppUserVo">
        select user_id, username, password, nickname, avatar, sex, phonenumber, status, del_flag, login_ip, login_date, credit_score, balance, frozen_deposit, create_by, create_time, update_by, update_time, remark from app_user
    </sql>

    <select id="selectAppUserList" parameterType="com.zNova.common.core.domain.entity.AppUser" resultMap="AppUserResult">
        <include refid="selectAppUserVo"/>
        <where>
            <if test="username != null  and username != ''"> and username like concat('%', #{username}, '%')</if>
            <if test="password != null  and password != ''"> and password = #{password}</if>
            <if test="nickname != null  and nickname != ''"> and nickname like concat('%', #{nickname}, '%')</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
            <if test="loginIp != null  and loginIp != ''"> and login_ip = #{loginIp}</if>
            <if test="loginDate != null "> and login_date = #{loginDate}</if>
        </where>
    </select>

    <select id="selectAppUserByUserId" parameterType="Long" resultMap="AppUserResult">
        <include refid="selectAppUserVo"/>
        where user_id = #{userId}
    </select>

    <select id="selectAppUserByUserName" parameterType="String" resultMap="AppUserResult">
        <include refid="selectAppUserVo"/>
        where username = #{username}
    </select>

    <update id="updateLoginInfo" parameterType="map">
        update app_user set
                            login_ip = #{loginIp},
                            login_date = #{loginDate}
        where user_id = #{userId}
    </update>

    <insert id="insertAppUser" parameterType="com.zNova.common.core.domain.entity.AppUser" useGeneratedKeys="true" keyProperty="userId">
        insert into app_user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="username != null and username != ''">username,</if>
            <if test="password != null and password != ''">password,</if>
            <if test="nickname != null">nickname,</if>
            <if test="avatar != null">avatar,</if>
            <if test="sex != null">sex,</if>
            <if test="phonenumber != null">phonenumber,</if>
            <if test="status != null">status,</if>
            <if test="delFlag != null">del_flag,</if>
            <if test="loginIp != null">login_ip,</if>
            <if test="loginDate != null">login_date,</if>
            <if test="creditScore != null">credit_score,</if>
            <if test="balance != null">balance,</if>
            <if test="frozenDeposit != null">frozen_deposit,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="remark != null">remark,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="username != null and username != ''">#{username},</if>
            <if test="password != null and password != ''">#{password},</if>
            <if test="nickname != null">#{nickname},</if>
            <if test="avatar != null">#{avatar},</if>
            <if test="sex != null">#{sex},</if>
            <if test="phonenumber != null">#{phonenumber},</if>
            <if test="status != null">#{status},</if>
            <if test="delFlag != null">#{delFlag},</if>
            <if test="loginIp != null">#{loginIp},</if>
            <if test="loginDate != null">#{loginDate},</if>
            <if test="creditScore != null">#{creditScore},</if>
            <if test="balance != null">#{balance},</if>
            <if test="frozenDeposit != null">#{frozenDeposit},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="remark != null">#{remark},</if>
        </trim>
    </insert>

    <update id="updateAppUser" parameterType="com.zNova.common.core.domain.entity.AppUser">
        update app_user
        <trim prefix="SET" suffixOverrides=",">
            <if test="username != null and username != ''">username = #{username},</if>
            <if test="password != null and password != ''">password = #{password},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="sex != null">sex = #{sex},</if>
            <if test="phonenumber != null">phonenumber = #{phonenumber},</if>
            <if test="status != null">status = #{status},</if>
            <if test="delFlag != null">del_flag = #{delFlag},</if>
            <if test="loginIp != null">login_ip = #{loginIp},</if>
            <if test="loginDate != null">login_date = #{loginDate},</if>
            <if test="creditScore != null">credit_score = #{creditScore},</if>
            <if test="balance != null">balance = #{balance},</if>
            <if test="frozenDeposit != null">frozen_deposit = #{frozenDeposit},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="remark != null">remark = #{remark},</if>
        </trim>
        where user_id = #{userId}
    </update>

    <delete id="deleteAppUserByUserId" parameterType="Long">
        delete from app_user where user_id = #{userId}
    </delete>

    <delete id="deleteAppUserByUserIds" parameterType="String">
        delete from app_user where user_id in
        <foreach item="userId" collection="array" open="(" separator="," close=")">
            #{userId}
        </foreach>
    </delete>

    <!-- 信用分增量更新（避免读-改-写并发问题） -->
    <update id="updateCreditScoreDelta">
        update app_user 
        set credit_score = GREATEST(0, IFNULL(credit_score, 500) + #{delta})
        where user_id = #{userId}
    </update>

    <!-- 冻结押金：增加用户的冻结押金金额 -->
    <update id="freezeDeposit">
        update app_user 
        set frozen_deposit = IFNULL(frozen_deposit, 0) + #{amount}
        where user_id = #{userId}
    </update>

    <!-- 解冻押金：减少冻结押金，增加可用余额（用于归还确认后返还押金） -->
    <update id="unfreezeDeposit">
        update app_user 
        set frozen_deposit = GREATEST(0, IFNULL(frozen_deposit, 0) - #{amount}),
            balance = IFNULL(balance, 0) + #{refundAmount}
        where user_id = #{userId}
    </update>

    <!-- 扣除冻结押金（逾期扣款时使用，不返还到余额） -->
    <update id="deductFrozenDeposit">
        update app_user 
        set frozen_deposit = GREATEST(0, IFNULL(frozen_deposit, 0) - #{amount})
        where user_id = #{userId}
    </update>
</mapper>